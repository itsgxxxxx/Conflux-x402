# Attestor Service Configuration

# Attestor private key (used to sign attestations)
ATTESTOR_PRIVATE_KEY=0xYourPrivateKeyHere

# Service port
PORT=3003

# Default expiry time in seconds (default: 30 days)
DEFAULT_EXPIRY_SECONDS=2592000

# Optional: Enable mock mode for testing (bypasses HTTP verification)
# MOCK_MODE=true
