{
  "name": "@conflux-x402/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Smart contracts for identity-based client authentication",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts",
    "deploy:registry": "hardhat run scripts/deploy-registry.ts",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@typechain/ethers-v6": "^0.5.1",
    "@typechain/hardhat": "^9.1.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.6.0",
    "@types/node": "^20.0.0",
    "ethers": "^6.13.0"
  }
}
