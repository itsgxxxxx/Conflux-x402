{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @conflux-x402/chain-config build && pnpm --filter @conflux-x402/express-middleware build && pnpm --filter @conflux-x402/moviememo build"
  },
  "deploy": {
    "startCommand": "cd examples/moviememo && node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
