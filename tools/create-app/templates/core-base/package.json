{
  "name": "{{PROJECT_NAME}}",
  "private": true,
  "version": "0.1.0",
  "description": "Conflux eSpace x402 payment application",
  "scripts": {
    "build": "pnpm -r build",
    "dev:facilitator": "pnpm --filter @{{PROJECT_NAME}}/facilitator dev",
    "dev:sandbox": "pnpm --filter @{{PROJECT_NAME}}/sandbox dev",
    "start:client": "pnpm --filter @{{PROJECT_NAME}}/client start",
    "start:client:manual": "pnpm --filter @{{PROJECT_NAME}}/client start:manual",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
